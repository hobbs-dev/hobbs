[package]
name = "hobbs"
version = "0.4.4"
edition = "2021"

[lib]
name = "hobbs"
path = "src/lib.rs"
crate-type = ["staticlib", "rlib"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
overflow-checks = false
debug = false

[dependencies]
